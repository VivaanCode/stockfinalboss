<!DOCTYPE html>
<html>
<head>
    <title>Stock Final Boss - Join</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <style>
        .login-card {
            background: #1f2937;
            border: 1px solid #374151;
            padding: 40px;
            border-radius: 16px;
            width: 100%;
            max-width: 400px;
            text-align: center;
        }
        input {
            width: 100%;
            padding: 12px;
            margin: 20px 0;
            background: #374151;
            border: 1px solid #4b5563;
            border-radius: 8px;
            color: white;
            font-size: 1rem;
            box-sizing: border-box;
        }
        .btn-join {
            width: 100%;
            padding: 12px;
            background: #3b82f6;
            color: white;
            font-size: 1rem;
        }
        .btn-join:hover { background: #2563eb; }
        .host-link {
            display: block;
            margin-top: 20px;
            color: #9ca3af;
            text-decoration: none;
            font-size: 0.9rem;
        }
        .host-link:hover { color: white; }
    </style>
</head>
<body style="display: flex; align-items: center; justify-content: center; min-height: 100vh;">
    <div class="login-card">
        <h1 style="margin-top:0">Stock Final Boss</h1>
        <p style="color: #9ca3af">Enter your username to start trading</p>
        <form action="/game" method="GET">
            <input type="text" name="username" placeholder="Username" required autocomplete="off">
            <button type="submit" class="btn-join">Start Playing</button>
        </form>
        <a href="/host" class="host-link">Leaderboard</a>
    </div>
</body>
</html>
